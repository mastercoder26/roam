# Secrets must never enter the build context, let alone an image layer.
.env
.env.*

node_modules/
dist/
.vercel/

# Tests and their fixtures run in CI, they are not shipped in the runtime image.
**/__tests__/
vitest.config.ts

.git/
.gitignore
*.md
